{
  "name": "llm-cost-meter-cloud",
  "version": "0.1.0",
  "private": true,
  "description": "Cloud API backend for llm-cost-meter",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
